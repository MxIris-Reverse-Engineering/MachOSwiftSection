# 从 ObjC 方法表还原 Swift 成员的 ObjC 事实：`@objc`、`override`、显式 selector

> 提案 [0047-objc-ancestor-override-recovery](../Evolutions/0047-objc-ancestor-override-recovery.md)（`override`）与 [0048-objc-member-selector-recovery](../Evolutions/0048-objc-member-selector-recovery.md)（`@objc` 与显式 selector，把前者推广到类的每一条 ObjC 方法）的实现说明。读者：维护者。为什么 Swift 元数据里没有这些事实、判据怎么推导的在提案里，本文只讲落地后的形状、证据档位在代码里的位置、以及边界。

## 一句话

一个 Swift 类的 ObjC 方法表就是它的 `@objc` 成员清单：每条一个 selector 加一个 IMP。库把每条方法联结到实现它的 Swift 成员，得到 per-class 的 **ObjC 成员表**（`ObjCMemberTable`），从中读出三件 Swift 元数据不记的事实：这个成员是 `@objc`（strip 掉 `To` thunk 符号的 OS 框架上，这是唯一证据——macOS 26.6 的 AppKit interface 里成员级 `@objc` 从 0 变为正数）；它 `override` 了哪个祖先的成员（selector 在祖先链上有人实现；编译器给这种覆写发的是新的普通 vtable 项，`@implementation` 类连 vtable 都没有）；它的 selector 是不是源码里 `@objc(name)` 写出来的（与编译器从 Swift 名正向推出的默认值不同）。

## 落地形状

| 层 | 文件 | 做什么 |
|---|---|---|
| SwiftInspection | `ObjCClassHierarchy.swift` | 值类型 `ObjCClassHierarchy`（类自己的方法：selector、实例/类、IMP 位置——含本镜像 category 的；祖先列表：名字 + 实例/类 selector 集合 + 该祖先采纳协议的 selector 集合；链是否走完；类自己采纳协议的 selector 集合 `adoptedProtocolSelectors`，含 `isComplete`；`adoptedProtocolDeclares` 与 `isAdoptedProtocolSetComplete` 连祖先一起算）；接缝协议 `ObjCClassHierarchyProviding`（class-bound）；按镜像**弱引用**的注册表 `ObjCClassHierarchyProviderStore`；驱逐入口 `ObjCClassHierarchies.removeCache(for:)`。 |
| SwiftInspection | `ObjCClassMethodIndex.swift` | 库自己的读取器，`SharedCache` 子类。急切部分读 classlist 与 `class_ro_t.name` 建「运行时名 → class object」「Swift 限定名 → 运行时名」两张表，再扫 `__objc_catlist` 按目标类名归组 category；方法表、祖先 selector 集合、协议 selector 按 class object 惰性求值、锁保护 memo；祖先在别的镜像时 memo 落在那个镜像自己的 `Storage`。目标类不在本镜像的 category（SwiftUI 给 `NSView` 加的）按目标类名另立一张 hierarchy，能跟着 class 指针走到 cache / 进程内的类时连祖先链一起给出。独立文件上 `.unresolvable(name)` 的父类与 bind 的 category 目标类交给镜像的 `ObjCAncestorResolver` 按名字续链；hierarchy 的 memo 键带解析器身份（`Storage.HierarchyMemoKey`）；`completingAncestors(of:in:)` 给 provider 交出的断链续上同一条尾巴。 |
| SwiftInspection | `ObjCAncestorResolver.swift` | 提案 `objc-ancestor-dependency-closure`：`ObjCAncestorResolver`（惰性求值的依赖镜像列表——根的传递闭包；`classObject(named:)` 先问每个镜像的 export trie 有没有 `_OBJC_CLASS_$_<name>`，有才建那个镜像的名字表；命中 / 未命中都按名字 memo；`.empty` 是「没有镜像」）与按镜像的注册表 `ObjCAncestorResolverStore`（indexer 与 `dump` 注册，无人注册的文件首次查询时得到系统 cache 上的默认解析器；`MachOImage` 永远为 nil）。 |
| SwiftInspection | `ObjCImplementationClassIndex.swift` | 读取器协议 `ObjCImplementationClassReading`（MachOFile / MachOImage 两份实现）：`superclassLocation(of:)`（`.root` / `.resolved(reader, class)` / `.unresolvable(name)`；旧 bind 格式的槽位经 MachOKitExtensions 的 `resolveBind(fileOffset:)` 取名，Swift 类永不当根）、category 的目标类名（`targetClassName(of:)`，旧 bind 格式同样走 `resolveBind`）/ 目标类 / 实例与类方法表 / 协议表、类与 category 的协议 selector（`RawObjCProtocolSelectors`，沿协议继承链递归；跟不到的 bind、以及读成**空字符串**的 selector——归档 cache 里另一镜像的协议方法表，名字串读不到——都标 `isComplete = false`）。 |
| SwiftInspection | `ObjCMember.swift` | `ObjCMember`（所属类名、selector、实例/类、`overriddenAncestorClassName?`、`evidence` 三档、`hasExplicitSelector`；`isInferredFromSelectorName` = 第三档，`isJoinedOverride` = 覆写且非第三档——消费者按证据强度过滤就靠这两个）与 `ObjCMemberTable`（按符号名索引的成员事实 + 联结不上的 `unattributedMethods` + 覆写投影 `overrides` / `unattributedOverriddenMethods` + 第三档的 `inferredOverrides(forMemberShapes:)`）。 |
| SwiftInspection | `ObjCMemberShape.swift` | 一个 Swift 成员对 selector 而言的形状（基名、标签、参数个数 / 属性名 / subscript accessor / 是否类型级 / 所属类型 / `throws` / `async`），从 demangle 树建（`To` thunk 符号的 `objCAttribute` 子节点跳过）。两套规则：`isConsistent(withSelector:isClassMethod:)` 是 importer 命名规则的**正向**检查（有损，做守卫）；`defaultSelector()` / `isDefaultSelector(_:)` 是编译器 `getObjCSelector` 的移植（无损，判显式 selector），含 30 个介词的 `prepositions` 与驼峰分词 `CamelCaseWords`。 |
| SwiftInspection | `NodeTypeNaming.swift` | 从 SwiftLayout 下沉（`package`），`nominalQualifiedName(ofDemangledRoot:)` 与 `swiftClassQualifiedName(fromRuntimeName:)`，ObjC 侧与 Swift 侧的类名 key 用同一个函数算。 |
| SwiftThunkAnalysis | `ObjCMembers/ObjCMembers.swift` | 门面与成员表构建器：`table(forObjCClassNamed:in:)` / `table(forSwiftClassQualifiedName:in:)`，先问注册的 provider 再走库读取器。放这里而不是 SwiftInspection，因为第二档要用本模块的 Capstone 解码器。 |
| SwiftThunkAnalysis | `ObjCMembers/ObjCMethodThunkReferences.swift` | 把一个 IMP 的代码反汇编，收 `bl` / 尾调 `b` 的目标与 `adrp`+`add` / `adr` 物化出的地址，换成本镜像的 Swift 符号名。MachOFile 与 MachOImage 两条腿；仅 ARM64。 |
| SwiftDeclaration | `FunctionDefinition` / `VariableDefinition` / `SubscriptDefinition` 的 `objcMember: ObjCMember?`；`AccessorRepresentable` 的同名要求 | `isOverride` 与 `isClassMember` 各 OR 上 `objcMember?.isJoinedOverride`——**只认前两档**：覆写的类方法打 `class`，`override static` 不是合法 Swift。`FieldDefinition` 恒为 `nil`（存储属性不能覆写，也不以字段名进方法表）。 |
| SwiftDeclaration | `ResolvedObjCMemberFacts.swift` | 第三档的裁决点。`resolvedObjCMemberFacts(trustingSelectorNameEvidence:)` 三个定义各一份，按消费者的裁决一次性算出 `objcMember` / `attributes`（采信时补 `@objc`）/ `isOverride` / `isClassMember` / `isFinal`（采信时压制——ObjC 运行时派发的方法是 `@objc dynamic`，不可能 final）。传 `false` 与定义自己的属性逐字相同。 |
| SwiftDeclaration | `Building/ObjCMemberApplication.swift`、`TypeDefinition+ObjCMembers.swift`、`ExtensionDefinition+ObjCMembers.swift` | 把表 join 到成员定义上：函数按 `symbol.name`，属性 / 下标**先按 getter** accessor（显式 selector 是 getter 的名字，只经 setter 联结上的成员把 `hasExplicitSelector` 清掉），`init` 按 allocator 符号换 initializer 后缀；**前两档**联结上的成员缺 `.objc` 属性就补上；第三档**始终**对仍未标的成员按形状唯一匹配覆写，但只写 `objcMember`、**不碰 `attributes`**——写进去的 `@objc` 会被紧接着跑的 `final` 还原读成 `@objc dynamic` 的证据，而那是消费者还没下的裁决。`TypeDefinition.index(in:)` 在 `applyThunkAttributes` 之后、`recoverFinalMembers` **之前**调用——`final` 还原把「`@objc` 且没有 descriptor」当 `@objc dynamic` 排除，strip 后的 `@objc` 就是这里补的。 |
| SwiftIndexing | `SwiftDeclarationIndexer.indexExtensions()`、`registerObjCClassHierarchyProvider(_:)`、`deinit` | `__C` 类的 extension（`@implementation` 主体、category、对外部类的 category）与本镜像 Swift 类的 extension（`@objc` 成员编成的 category）都过一遍表；宿主注册 provider 的便利入口；随符号表一起驱逐。`registerDependencyClosureConsumers()` 在索引类型之前用一个 `SharedDependencyClosure` 同时注册 property-wrapper catalog 与祖先解析器（同一次闭包求值），搜索路径打不开时派发 `renderingDegraded(.dependencyLoad)`；解析器按独立 claim 随最后一个活着的 indexer 驱逐。 |
| MachODependencies | `DependencyPlatforms.swift`、`FileDependencyLocator.swift`、`SharedDependencyClosure.swift` | 平台守卫：cache 镜像的 `LC_BUILD_VERSION`（没有则 `LC_VERSION_MIN_*`）平台集合与根镜像不相交就不入定位器的一次性索引（macOS cache 里 `/System/iOSSupport` 的 Catalyst UIKit 不再被 iOS 根按裸名兜底拿到）；显式文件与 system root 不过滤。`SharedDependencyClosure` 一次求值多方共享。 |
| swift-section | `DumpCommand.dump()`、`MachOOptionGroup` | `dump` 没有 indexer，自己按 `--dependency-search-path`（+ 系统 cache）注册解析器；帮助文本写明该选项也管 ObjC 祖先链。 |
| SwiftIndexing | `ObjCInterfaceIndexerClassHierarchyProvider.swift` | ObjCSection 索引器的适配器（`ObjCIndexing.ObjCInterfaceIndexer` → provider，按目标类名归组 indexer 另存的 category）与 `ObjCClassHierarchy.init(classInfoChain:categoryInfos:isAncestorChainComplete:)`（协议 selector 从 `ObjCClassInfo.protocols` 递归收，`isComplete = true`），RuntimeViewer 让自己的索引器遵循协议时一行转换。indexer 不认识的类答 nil，库读取器接手——对外部类的 category 也是。 |
| SwiftPrinting | 三个成员 printer、`@implementation` 存储属性 printer、`renderMember` 的 export-status 腿、`+ExportFilter` | 每处成员读取都先取 `resolvedObjCMemberFacts(trustingSelectorNameEvidence: trustsSelectorNameEvidence)`（后者来自 `SwiftDeclarationPrintConfiguration.infersObjCOverridesFromSelectorNames`），`@objc` / `@objc(selector)` / `override` / `class` / `final` / export-status 豁免全读它——四个关键字同进同退，不会出现「标了 override 又标 final」。 |
| SwiftDump | `ClassDumper`、`ObjCImplementationClassDumper`、`ObjCMemberRendering.swift` | 类头下一行 `// ObjC ancestor chain: NSView → NSResponder → NSObject`（断链标 `(bound; chain not resolvable offline)`），成员符号行尾 `// overrides -[NSView layout] (<证据>)` 或 `// @objc -[Class selector][, explicit selector] (<证据>)`；`@implementation` 的 ObjC 方法行 `overrides NSView` / `… (no Swift member tied to this IMP)` / `no Swift member tied to this IMP`；export-status 注释对表里的成员豁免。 |

## 三档证据在代码里的位置

联结全在 `ObjCMembers.table(for:ownerQualifiedName:in:)` 的循环里，对类自己方法表中的**每一条**（不再先按祖先 selector 过滤）：

1. **`To` 符号在 IMP 处**（`evidence: .thunkSymbol`）：`machO.symbols(offset:)` 在 IMP 处找到 Swift 符号，就按那个符号名（`$s…FTo`、`$s…vgTo`、`$s…fcTo`）登记。同一 IMP 处有多个符号（identical code folding 把两个成员的 thunk 叠在一起）时，只登记形状与 selector 一致的那些；一个都不一致才全部登记。成员定义查表时拿自己的符号名加 `To`（allocator `…fC` 换成 initializer `…fcTo`）。只对没 strip 的二进制成立。
2. **thunk 引用成员实现**（`.thunkReference`）：IMP 处没符号时，`ObjCMethodThunkReferences` 反汇编那段代码，把它引用的每个地址换成 Swift 符号名，逐个过两道守卫——`ObjCMemberShape.ownerQualifiedName` 必须等于这个类（`__C.NSGlassEffectView` 或 Swift 限定名），`isConsistent(withSelector:)` 必须成立——通过的按**成员实现的符号名**登记（`$s…F`、`$s…vs`、`$s…fc`），成员定义查表时裸名也能命中。IDA 核实的三种形状：`bl $s…layoutyyF`（普通方法与 `init`）、`adrl x16, $s…FZ; pacia; mov x3, x16; b outlined`（类方法，地址物化给 outlined helper）、setter 同 `bl`。**显式 selector 的成员过不了第二道守卫**（`pokeUsingForce:` 不是任何 importer 对 `poke(force:)` 的拼法），所以 strip 后它连 `@objc` 都拿不到——诚实漏标，不错标；fixture 的 `.strippedLocals` 变体把这一点固定成了测试。
3. **只按名字**（`.selectorName`，只对覆写）：前两档都联结不上的方法进 `unattributedMethods`；interface 侧 `ObjCMemberApplication.inferFromSelectorNames` 收集类里**尚未标记**的成员形状，dump 侧 `ObjCMemberRendering.inferredOverrides(for:memberSymbols:)` 收该类所有成员符号里不在表里的形状（键是符号名），两边都交给 `table.inferredOverrides(forMemberShapes:)` 给每个未联结的**覆写**方法找一致的成员，**恰好一个**才归属。

   **这一档始终跑**（2026-09-22 起的第二次改动）。索引只负责记录它找到了什么，用不用是消费者的事：dump 每条联结都标证据，所以这一档写成 `(selector name, no symbol evidence)` 照常渲染；interface 只有 `override` 这一个关键字、没地方说明它靠的是名字，所以看 `SwiftDeclarationPrintConfiguration.infersObjCOverridesFromSelectorNames`（CLI `swift-section interface --infer-objc-overrides`），默认关。索引期的按镜像开关（`ObjCMemberRecoveryOptions` / `ObjCMemberRecoveryOptionsStore` / `SwiftDeclarationIndexConfiguration.infersObjCOverridesFromSelectorNames`）与 `dump` 上的同名 flag 都已删除。

   NSGlassEffectView 剩下的 6 个（`renewGState` / `viewDidHide` / `viewDidUnhide` / `_windowChangedKeyState` / `_viewDidChangeEffectiveCornerRadii` / `encode(with:)`）全是这一档：方法体是 `super.xxx()` 被内联成 `objc_msgSendSuper` 或一个 outlined helper，thunk 里不剩任何 Swift 符号引用。

`ObjCMemberShape.isConsistent` 的规则（importer 的正向拼法）：零参方法 / getter 与 selector 相等；setter 是 `set` + 属性名首字母大写，`is` 前缀可省（`isEnabled` ↔ `setEnabled:`）；n 参方法 selector 恰有 n 段，第一段以基名开头、去掉基名后小写首字母以第一个标签开头（`viewWillMoveToWindow:` ↔ `viewWillMove(toWindow:)`、`encodeWithCoder:` ↔ `encode(with:)`），后续每段小写首字母以对应标签开头（`withObject:` ↔ `with:`），无标签参数接受任何段；`init` 同上并跳过开头的 `With`；subscript 的 accessor 接受运行时的四个固定 selector。参数个数从 function type 的 argument tuple 数，不从 labelList 数——全无标签时 demangler 不发 labelList。APINotes 或 `@objc(name)` 改过名的成员通不过检查，只会漏标不会错标。

## 显式 selector 的判据

`ObjCMemberShape.defaultSelector()` 是 `lib/AST/Decl.cpp` `AbstractFunctionDecl::getObjCSelector` 与 `VarDecl::getDefaultObjCSetterSelector` 的移植：无参 = 基名；单个无标签参数 = `基名:`；有标签的第一段 = 基名 +（`With`，除非第一个标签的首词或基名的末词是介词）+ 首字母大写的标签，后续段 = 标签原文（无标签为空段）；`throws` 追加 `error:`（无参时 `基名AndReturnError:`），`async` 追加 `completionHandler:`（无参时 `基名WithCompletionHandler:`），`async throws` 只追加后者；`init` 是基名为 `init` 的方法；getter = 属性名，setter = `set` + 首字母大写的属性名（**没有** `is` 处理，那是 importer 的规则）。介词表是 `lib/Basic/PartsOfSpeech.def` 的 30 个词；驼峰分词按 `camel_case::Words`（全大写连续段视为一个缩写词，`URLSession` → `URL` / `Session`）。

`hasExplicitSelector` = 实际 selector ≠ 默认推导，**且**不是覆写（selector 从被覆写者继承）、**且**不是 `@objc` 协议要求的 witness（selector 从要求继承——类自己采纳的协议**和每个祖先采纳的协议**都算，conformance 是继承的：`NSTableView` 的 `NSDraggingSource` 决定子类 `draggingSession(_:movedTo:)` 的 selector）、**且**继承的两个来源都读完了——`isAncestorChainComplete` 与 `isAdoptedProtocolSetComplete`（类与每个祖先的协议集合都完整）同时成立。磁盘上独立二进制的父类与 SDK 协议是 bind，读不到，就**不下判定**：第一轮 A/B 在模拟器运行时的 SwiftUI 文件上把 `hitTest:withEvent:` / `drawRect:` 这类 UIKit 覆写全判成了显式 selector，合法但误导。dump 注释里 selector 照样写出，只是不加 `explicit selector`。第一轮 A/B 还抓到另一种「读完了但没读对」：macOS 15.5 归档 cache 里 WidgetKit 的类采纳 Foundation 的 `NSSecureCoding`，跨镜像读它的方法表得到的 selector 全是空字符串，集合看似完整、`encodeWithCoder:` 却不在其中，于是 witness 被判成显式——空 selector 现在一律视为读失败、集合标不完整。`@objc @implementation` 体**不是**例外：编译器同样从 Swift 名推导并要求头文件声明了那个 selector（`draw(in:)` 推出 `drawIn:`，头文件只有 `drawInRect:` 就报错），所以 AppKit 里 `NSGradient` 的 `draw(in:angle:)` 对 `drawInRect:angle:` 是源码写了 `@objc(drawInRect:angle:)`。interface 打 `@objc(selector)`，属性用 getter 的 selector；dump 在注释里加 `, explicit selector`。

## 三个 join 的键

- **Swift 类 ↔ ObjC class object**：`TypeDefinition.typeName.node` 物化后 `NodeTypeNaming.nominalQualifiedName(ofDemangledRoot:)`，与 `ObjCClassMethodIndex` 对 `_TtC…` / `$s…` 运行时名 demangle 后算的限定名是同一个函数。同名私有类落进同一个 key 时（两条运行时名）拒绝归属并 `#log`。泛型类不在 classlist 里，查不到就什么都不标。
- **extension ↔ 表**：`__C.X` 的 extension 按裸名 `X`（`@implementation` 主体、category、对外部类的 category 共用一张表）；本镜像 Swift 类的 extension 按限定名，与类本体同一张表——category 成员本就折进了类的 hierarchy。
- **成员定义 ↔ 表**：按符号名，见上。属性 / 下标任一 accessor 命中即整个成员标记，getter 优先。

## 边界与已知限制

- **泛型 ObjC 派生类**（SwiftUI 26 个）无静态 class object，不标。它们的 `class_ro_t` 在泛型 metadata pattern 的 extra-data 块里，留待后续。
- **iOS 18.5 及之后的模拟器运行时文件**：UIKitCore / Foundation 的 `__objc_classlist` 有约六分之一读不出（探针实测 UIKitCore 5017 项里 791 个 `class_ro_t` 读不到、624 个被误读为元类），`UIView` / `UIResponder` 这类根类恰在其中，解析器查不到它们的 class object，链在那里断并标 `(bound; chain not resolvable offline)`；iOS 15.5（旧 rebase 格式）大部分读得出。是 MachOKit / MachOObjCSection 对这批 chained-fixup 文件的读取问题，不在成员表这一层。
- **磁盘上的独立二进制**：父类在别的镜像时是 bind，`superclassLocation` 返回 `.unresolvable(name)`；2026-09-21 起这个名字交给镜像的祖先解析器在依赖闭包里找（见下一节），找不到——依赖镜像不在宿主上、iOS 二进制在 macOS 宿主上没给 `--dependency-search-path`、`.unresolvable(nil)`（Swift 类的槽位为零又没有 bind 名）——链才在那里断；祖先的 category 若在闭包够不到的镜像里（闭包报为未解析的依赖），那个 category 的方法看不见，覆写它的成员会被判成显式 selector（fixture 的 `categoryInAnUnreachableImageIsInvisible` 钉住这条边界）；断了的话同镜像祖先照常判，显式 selector 一律不判。旧的 `LC_DYLD_INFO` bind 格式（部署目标低于 macOS 12 / iOS 16——iOS 15.5 模拟器运行时的全部框架）把 bind 槽位留成 0，MachOObjCSection 把它读成「没有父类」；读取器补了一道 MachOKitExtensions 的 `resolveBind(fileOffset:)`（两种格式都认）取 bind 符号名，再兜一道「Swift 类不可能是 ObjC 根类」，否则链会被误判为走完、每个 UIKit 覆写都成了显式 selector（第一轮 A/B 的模拟器腿就是这样）；这个名字现在也是解析器的输入。fixture 的 `.legacyBinds` 变体固定两种结果：空解析器下断在 `NSObject`，默认解析器下经宿主 cache 走完。
- **strip 后的显式 selector**：只有第一档能给；第二档的 importer 拼法守卫天然拒绝它（见上），第三档只管覆写。
- **仅 ARM64**：第二档的解码器与模块其余部分一样只认 ARM64，x86_64 镜像只有第一、三档。
- **`required` 不还原**：`init?(coder:)` 打成 `override init?(coder:)`，库本来就不还原 `required`。
- **`init` 与 `.cxx_destruct` 也在表里**：编译器给每个 ObjC 派生的 Swift 类合成的 `init` 与指向 ivar destroyer 的 `-.cxx_destruct` 都是方法表条目；前者联结到 allocator 定义（`@objc init()`，默认 selector `init` 不算显式），后者没有对应的成员定义，只在 dump 的符号行上留一条注释。
- **不进 ABI 快照**：`objcMember` 不在 `MemberRecord` 里，`formatVersion` 不变。
- **第三档始终索引，输不输出由消费者定**（2026-09-22）：用户的既定裁决「只联结不猜」管的是**输出**，不是索引。索引照常跑完三档并把结果记在 `Definition.objcMember` 上（`evidence == .selectorName`），dump 用 `(selector name, no symbol evidence)` 标出来源照常渲染，`@implementation` 的 ObjC 方法行写 `overrides NSView (selector name, no symbol evidence)`；interface 默认不打，`--infer-objc-overrides` 才打。它只补覆写，永远不会添出 `@objc(name)`；macOS 26.7 AppKit 的 38 个 `@implementation` 类里，96 条联结不上的覆写开了之后剩 65 条（`init` / `.cxx_destruct` 这类没有对应成员的、以及形状撞出多个候选的留下）。
- **第三档不写 `attributes`**：这是「始终索引」能成立的前提。`final` 还原紧跟在 `applyObjCMembers` 之后，把「有 `@objc` 且无 descriptor」读成 `@objc dynamic` 的证据；`--exported-only` 的过滤同样读 `attributes.contains(.objc)`。第三档若在索引期写下 `@objc`，这两个判断就先于消费者的裁决落定，而且收不回来。所以它只写 `objcMember`，四个关键字（`@objc` / `override` / `class` / 压制 `final`）统一由 `ResolvedObjCMemberFacts` 在渲染时一次性给出。

## 祖先链走依赖闭包（提案 `objc-ancestor-dependency-closure`）

cache 镜像与进程内镜像顺着父类指针就能跨镜像走到根；磁盘上的独立文件（app 二进制、抽出来的框架、iOS 26 及更早模拟器运行时的框架文件）父类是 bind，在这之前链在第一跳就断——UIKit / AppKit 的覆写一个都标不出，显式 selector 一律不判（fail closed），渲染 A/B 的四条模拟器腿没有一行 `override`。现在的走法：

1. **名字从哪来**：`superclassLocation(of:)` 的 `.unresolvable(name)`（chained fixups 由 MachOObjCSection 直接给名，旧 `LC_DYLD_INFO` 格式由 `resolveBind(fileOffset:)` 给），category 的目标类用 `targetClassName(of:)`。bind 符号 `_OBJC_CLASS_$_UIView` 去前缀就是 `class_ro_t.name`；Swift 父类的 `_OBJC_CLASS_$__TtC7SwiftUI9Something` 去前缀得 `_TtC…`，不需要二次转换。
2. **去哪找**：`ObjCAncestorResolver` 持有根的传递依赖闭包（`DependencyClosure(root:searchPaths:traversal: .transitive)`，BFS 顺序，惰性求值），对每个镜像先查 export trie 有没有 `_OBJC_CLASS_$_<name>`（bind 只能落到导出符号，这一步是精确预检，省掉绝大多数镜像的 classlist 扫描；umbrella 的 re-export 条目会通过预检但 classlist 落空，跟在闭包后面的真正定义者接住），有才取 `ObjCClassMethodIndex.shared.storage(in:)` 的名字表；第一个命中即返回，命中 / 未命中都按名字 memo。一个解析器服务整条链的每一跳：显式文件里找到的父类自己的父类又是 bind，根的传递闭包里也有那个镜像。
   **祖先的 category 来自别的文件**：cache 里的类，dyld 把同一 cache 内其它镜像的 category 预挂进它的 list-of-lists（cache 腿上 NSObject 的方法表里有 Foundation 的 KVO 方法）；文件世界里 category 只在加载时挂，离线读不到——模拟器腿上 `observeValue(forKeyPath:of:change:context:)` 曾因此被判成 `@objc(observeValueForKeyPath:ofObject:change:context:)`。祖先链的每一跳都把根镜像自己与闭包里每个独立文件（`!isLoadedFromDyldCache`）对该祖先的 category 并进它的 selector 集合与协议集合（`ObjCAncestorResolver.fileCategorySelectors(onClassNamed:)`，按名字 memo，首次给闭包里每个文件建名字表；根的 category 由 `ObjCClassMethodIndex.AncestorCategoryFolding` 读）。cache 镜像的 category 不折。进程内同理：`dlopen` 进来的非 cache 镜像的 category 被运行时挂进 `class_rw_ext_t`，两个读取器只读 `class_ro_t`，所以 `MachOImage` 根也有一个只做折入的解析器（`ObjCAncestorResolver(inProcessRoot:)`，走 `DependencyClosure(root: MachOImage)`，只看 `!header.isInDyldCache` 的镜像）。
3. **谁装的**：`ObjCAncestorResolverStore` 按镜像登记。`SwiftDeclarationIndexer.index()` 用配置的 `dependencySearchPaths` 注册（与 property-wrapper catalog 共用一个 `SharedDependencyClosure`，闭包只求值一次；打不开的搜索路径派发 `renderingDegraded(.dependencyLoad)`），`swift-section dump` 自己按 `--dependency-search-path` 注册，无人注册的文件在首次查询时得到一个系统 cache 上的默认解析器——与 catalog 的契约相同，所以只走 dump 路径的宿主不用改；`MachOImage` 永远没有解析器。闭包的搜索路径与 catalog 一直以来的完全相同（落地时试过让 cache 内的根先查自己的 cache，A/B 抓到 catalog 因此在 `cache-15.5` 腿丢掉 `@IdentityLink`，撤回——见提案决策日志）。indexer 的 `deinit` 按独立的 claim 随最后一个活着的 indexer 驱逐，`ObjCClassHierarchies.removeCache(for:)` 也清它。
4. **memo**：hierarchy 的 memo 键是 (class object 偏移或外部类名, 解析器身份 `UUID`)——同一个 class object 在不同解析器下的链不同；祖先 selector 集合不走链、键不变。
5. **provider 的断链也续**：宿主的 ObjC 索引器（RuntimeViewer、`ObjCInterfaceIndexer` 适配器）在 bind 处停下的位置与库读取器相同，`ObjCMembers` 对 provider 的结果调 `ObjCClassMethodIndex.completingAncestors(of:in:)`，从 `unresolvedAncestorName` 用同一个解析器续，两条接缝在独立文件上得到同一条链。provider 协议不变。
6. **平台守卫**（MachODependencies）：iOS 二进制的 `/System/Library/Frameworks/UIKit.framework/UIKit` 在 macOS cache 里没有精确匹配，裸名排序会落到 `/System/iOSSupport` 的 Catalyst 版——同名类、不同平台、祖先 selector 集合不同，`override` 会错标。`FileDependencyLocator` 一次性建 cache 索引时按 `DependencyPlatforms.platforms(of:)`（全部 `LC_BUILD_VERSION` 的平台，没有则按 `LC_VERSION_MIN_*` 推；空集合放行）过滤与根不相交的镜像，精确路径与裸名两步一起受约束；显式文件与 system root 不过滤。被拒的 load name 落进 `unresolvedLoadNames`。布局引擎与 `__C` 归属走同一个定位器，一并受益——也意味着 iOS 二进制在 macOS 宿主上不带搜索路径时它们不再拿到 Catalyst 镜像。
7. **渲染 A/B**：模拟器腿对两侧都传 `--dependency-search-path <RuntimeRoot>`（≤ iOS 26 的运行时把框架当文件放在那棵树里，system root 语义正好）；两侧输入一致，差异只剩解析器本身。

结果：fixture 的文件腿经宿主 cache 走到 libobjc 的 `NSObject`，`override var description` 与 `@objc(pokeUsingForce:)` 在文件上也标；链走完后编译器合成的 `init` 成了 `-[NSObject init]` 的覆写（与 cache 腿一直以来的行为一致）。`SymbolTestsCore` 快照里 `ExternalObjCSubclassTest.isKind(of:)` 从 `@objc func` 变成 `override func`——上一提案任务报告里「断链错判的最小样本」，现在按事实标。

## 验证

- 第三档（2026-09-22）：fixture 加 `.optimizedStripped` 变体（`-O` 再 `strip -x`——优化器把 `super.ping()` 这种小方法体内联进 thunk，IMP 不再引用任何 Swift 符号，前两档全部落空，正是 AppKit 的形状）；`ObjCMemberRecoveryTests` 固定 interface 默认下这些覆写不打、`infersObjCOverridesFromSelectorNames: true` 下 `ping` / `pingCount` / `level` / `description` / category dylib 里的 `noteValueForKeyPath:ofObject:` / extension 里的 `bump` / 孙类的 `dynamicHook` / `@implementation` 体的三个都标 `override`，而 `notAnOverride` 与显式 selector 的 `poke(force:)` 不被碰。
- 「始终索引」的契约（`indexRecordsTheNameOnlyTieWhicheverWayTheConsumerRules`）：没有任何开关的情况下 `ping.objcMember` 就在那里且 `evidence == .selectorName`，同时 `isOverride` 为 `false`、`attributes` 不含 `.objc`；`resolvedObjCMemberFacts(trustingSelectorNameEvidence: true)` 一次性给出 `isOverride` / `isObjC` / `!isFinal`，传 `false` 则逐字复现定义自己的取值。这条测试是「关掉时输出不变」与「打开时 `final` 被压制」两件事唯一的复现点。
- `ObjCMemberDumpTests` 固定两个 dumper **默认**就写 `(selector name, no symbol evidence)`（dump 不再有开关）。`InferObjCOverridesFlagTests`（SwiftSectionCommandTests）固定 `interface` 的 flag 默认关、`dump` 传这个 flag 直接报错。
- 渲染 A/B（2026-09-22「始终索引」那次）：归档 cache 15.5 的 6 个框架 × dump + interface 共 12 对逐字节一致；macOS 26.6.2 的 AppKit 手工三组——两侧 `interface` 默认逐字节一致、两侧 `interface --infer-objc-overrides` 逐字节一致（**这条证明 printer 现场派生的四个关键字与旧的索引期实现等价**）、基线 `dump --infer-objc-overrides` 与候选默认 `dump` 逐字节一致；基线默认 `dump` 与候选默认 `dump` 的 62 处差异全是 `(selector name, no symbol evidence)`，零例外。注意 harness 的 `ARCHIVED_CACHE_DIRECTORIES` 写的是 `macOS/26.6`，磁盘上是 `26.6.2`，26.x 那条腿在默认跑法下是跳过的。
- `ObjCMemberRecoveryTests`（SwiftInterfaceTests）：fixture 的普通 Swift 子类 / 孙类（`@objc dynamic` 基）/ `@implementation` 子类三种覆写，extension（category）里的覆写与 `@objc` 成员，反例，文件与进程内两条腿；`.strippedLocals` 变体上 `@objc` 与 `override` 靠第二档、`@objc dynamic` 不再被打成 `final`、显式 selector 诚实缺席；显式 selector 与默认推导（`throws` / `async` / 介词 / 无标签 / witness）的 interface 输出与表事实；provider 等价性（`ObjCInterfaceIndexer` 适配器 + spy，含 category 与协议）、弱注册、`ObjCClassInfo` 链转换（含 category 去重与协议 selector）、成员形状与两套规则的对照表、第三档只归属唯一候选。
- `AppKitObjCMemberTests`：系统 cache 的 AppKit，NSGlassEffectView 的链为 NSView → NSResponder → NSObject 且走完，覆写全由第二档给出且无一声称显式 selector，`clipsToBounds` getter 与 `viewDidHide` 在未联结列表里，协议 selector 在 cache 内读完整；macOS 26 以下跳过。
- `ObjCMemberDumpTests`（SwiftDumpTests）：两个 dumper 的链注释、`overrides` 注释、`@objc -[…]` 注释（含 `explicit selector`）、strip 后的证据档位；空解析器下链注释标 `(bound; chain not resolvable offline)` 且不判显式 selector。
- 祖先链走依赖闭包（`ObjCMemberRecoveryTests` 新增 / 改写）：fixture 现在多一个单独的 category dylib（`libObjCImplementationFixtureCategories`，头文件里声明 `ClangWidget (SeparatelyShipped)` 的 `noteValueForKeyPath:ofObject:`，每个变体都链接它；`ObjCImplementationFixture.dependencySearchPaths()` 给出「category dylib + 系统 cache」）；在这组搜索路径下文件腿链走完、显式 selector 与 `description` 覆写都标、`noteValue(forKeyPath:of:)` 经 category dylib 判为 ClangWidget 的覆写而不是 `@objc(name)`；只给系统 cache 时 category dylib 报未解析、该成员判成显式（边界钉住）；`dependencySearchPaths: []` 与 `ObjCAncestorResolver.empty` 下断在 `NSObject`、不判；`.legacyBinds` 两种解析器各一组；解析器本身在闭包里找到 libobjc 的 `NSObject`、未知名字与空解析器答 nil、注册表的注册 / 默认 / 移除；provider 交出的断链续到与读取器相同的 `["ClangWidget", "NSObject"]`。碰 fixture 的四个 suite 都挂 `ExclusiveImageAccess(ObjCImplementationFixture.moduleName)`。
- `FileDependencyLocatorTests`（MachODependenciesTests）：`LC_BUILD_VERSION` 平台读取（原生 SwiftUI 含 macOS，iOSSupport 副本只有 macCatalyst）、`areCompatible` 的相交 / 空集合规则、iOSSimulator 根在宿主 cache 上对 UIKit 与 `/usr/lib/libobjc.A.dylib` 都答 nil、macOS 根照常、zippered 根可取 Catalyst 镜像。
- 全量测试与渲染 A/B 结果见两份任务报告。
